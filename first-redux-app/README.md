nextjs のアプリに redux をインストールして 1 から作るほうが良い

```bash
npx create-next-app my-app
npm install redux
npm install react-redux
npm install @reduxjs/toolkit
npm install --save-dev @redux-devtools/extension
```
